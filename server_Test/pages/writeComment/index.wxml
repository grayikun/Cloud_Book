<view class="fb_main">
    <view class="fb_content">
      <textarea value="{{textVal}}" bindinput="handleTextInput" placeholder="请输入你的评论"></textarea>
        <view class="fb_tool">
          <!-- <button bindtap="handleChooseImg">+</button>
          <view class="up_img_item" wx:for="{{chooseImgs}}" 
          wx:key="*this" bindtap="handleRemoveImg" data-index="{{index}}">
          
          <UpImg src="{{item}}"></UpImg></view> -->
          <!-- <button bindtap="handleChooseAddress" type="primary" plain>获取收货地址</button> -->
        </view>
    </view>
    <view class="form_btn_wrap">
      <button bindtap="handleFormSubmit" >
        提交评论
      </button>
    </view>
  </view>


