<view class="cates">
  <SearchInput></SearchInput>
  <!-- 左边 -->
  <view class="cates_container">
  <!-- 右边 -->
    <scroll-view class="right_content" scroll-y scroll-top="{{scrollTop}}">
      <view class="goods_group" >
        <view class="goods_title">
          <text class="delimiter">/</text>
          <text class="title">收藏的图书</text>
          <text class="delimiter">/</text>
        </view>

        <view class="goods_list">
          <navigator wx:for="{{collect}}" wx:key="book_id" url="/pages/introduce/index?book_id={{item.book_id}}">
            <image mode="widthFix" src="{{item.img}}"></image>
            <view class="goods_name">{{item.book_name}}</view>
          </navigator>
        </view>
      </view>
    </scroll-view>

  </view>
</view>
